# Output Folder

All architecture documents generated by the CPMS Architect agent are stored here,
organised by workflow phase.

## Folder Structure

| Folder | Phase | Contents |
|--------|-------|---------|
| `c4-level-1-context/` | Level 1 | System Context diagram (.md + .drawio), ADR-001 |
| `c4-level-2-container/` | Level 2 | Container diagram (.md + .drawio), ADRs 002–006 |
| `c4-level-3-component/` | Level 3 | One .md + .drawio per microservice, OCPP sequence diagrams |
| `c4-level-4-data-models/` | Level 4 | PostgreSQL DDL, Kafka message schemas, Redis key catalog |
| `api-design/` | Level 5 | GraphQL SDL, gRPC protos, OCPI endpoint catalog, deployment diagram |
| `modules/` | On-demand | Deep-dive module/profile designs (one subfolder per module) |
| `decisions/` | All phases | Architecture Decision Records (ADR-NNN-*.md) |

## Document Conventions

Every output document:
1. Opens with a **summary paragraph** (audience + what it covers)
2. Has a **Decisions Made** section listing key choices
3. **Cites OCPP/OCPI spec sections** for every protocol-related decision
4. Contains at least one **Mermaid diagram** embedded in the Markdown
5. Has a corresponding **.drawio file** with the same diagram for draw.io
6. Lists **Open Questions** needing human input
7. Has a **Next Phase Preview** (except the final phase)

## Rendering Mermaid Diagrams

- **VS Code**: Install the "Mermaid Preview" or "Markdown Preview Mermaid Support" extension
- **GitHub / GitLab**: Renders automatically in `.md` files
- **CLI**: `npx @mermaid-js/mermaid-cli -i diagram.md -o diagram.svg`
- **Online**: https://mermaid.live

## Opening draw.io Files

- **Desktop**: https://www.drawio.com (free download) or https://app.diagrams.net
- **VS Code**: Install the "Draw.io Integration" extension (hediet.vscode-drawio)
- **Confluence**: draw.io is available as a Confluence plugin
- The `.drawio` files are plain XML — they can also be read programmatically

## ADR Index

Architecture Decision Records in `decisions/` are numbered sequentially:
- ADR-001: System Boundary (Level 1)
- ADR-002: OCPP Gateway Separation (Level 2)
- ADR-003: Kafka Event Backbone (Level 2)
- ADR-004: Redis Connection Registry (Level 2)
- ADR-005: GraphQL Federation (Level 2)
- ADR-006: Service Decomposition (Level 2)
- _(subsequent ADRs added per phase)_

## Workflow Status

Current phase and approval status are tracked in `workflow-state.json`
in the project root.
